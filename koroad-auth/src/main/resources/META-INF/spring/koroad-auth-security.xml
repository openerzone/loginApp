<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/security
                           http://www.springframework.org/schema/security/spring-security.xsd">

    <!-- Security Configuration: /auth/** 패턴만 처리 -->
    <security:http pattern="/auth/**" auto-config="true" use-expressions="true" >
        <!-- Login Configuration -->
        <security:form-login 
            login-page="/auth/signin"
            login-processing-url="/auth/signin"
            username-parameter="username"
            password-parameter="password"
            authentication-success-handler-ref="signinAuthenticationSuccesshandler" 
            authentication-failure-handler-ref="signinAuthenticationFailureHandler" />  

        <!-- 로그아웃 설정 -->
        <security:logout
            logout-url="/auth/logout"      
            success-handler-ref="logoutSuccessHandler" />
        
        <security:custom-filter ref="otpAuthenticationFilter" before="FORM_LOGIN_FILTER" />
        
        <!-- 비밀번호 변경 필요 필터 (인증 후 가장 먼저 실행) -->
        <security:custom-filter ref="passwordChangeRequiredFilter" after="BASIC_AUTH_FILTER" />
        
                         
		<!-- URL Pattern Access Control -->
		<security:intercept-url pattern="/auth/signin" access="permitAll()" />
		<security:intercept-url pattern="/auth/logout" access="isAuthenticated()" />
		<security:intercept-url pattern="/auth/otp" access="hasAnyRole('ROLE_2FA_PENDING')" />
		<security:intercept-url pattern="/auth/otp/verify" access="hasAnyRole('ROLE_2FA_PENDING')" method="POST"/>
		<security:intercept-url pattern="/auth/otp/failure" access="permitAll()" />
		<!-- <security:intercept-url pattern="/auth/otp/resend" access="isAuthenticated()" /> -->
		<security:intercept-url pattern="/auth/change-password" access="hasAnyRole('ROLE_PASSWORD_CHANGE_REQUIRED')" />
		<security:intercept-url pattern="/auth/change-password/process" access="hasAnyRole('ROLE_PASSWORD_CHANGE_REQUIRED')" />
        <!-- 
        <security:intercept-url pattern="/static/**" access="permitAll()" />
		<security:intercept-url pattern="/**" access="permitAll()" />
		 -->
		<security:csrf disabled="true"/>
    </security:http>

    <!-- Authentication Manager -->
    <security:authentication-manager id="authenticationManager">
        <security:authentication-provider ref="signinAuthenticationProvider" />
        <security:authentication-provider ref="otpAuthenticationProvider" />
    </security:authentication-manager>   
        
    <!-- EgovPasswordEncoder: EgovFileScrty.encryptPassword 메소드를 사용하는 인코더 -->
	<bean id="passwordEncoder" class="kr.or.koroad.auth.util.EgovPasswordEncoder" />
	
	<!-- OTP 서비스: 명시적으로 선언하여 Root Context에서만 생성되도록 보장 -->
	<bean id="otpService" class="kr.or.koroad.auth.service.OtpService" scope="singleton" />
	
	<!-- Custom AuthenticationProvider: 사용자 ID를 salt로 사용하는 비밀번호 검증 -->
	<bean id="signinAuthenticationProvider" class="kr.or.koroad.auth.security.provider.SigninAuthenticationProvider">
		<constructor-arg ref="passwordEncoder" />
		<property name="userDetailsService" ref="allbaroUserDetailsService" />
	</bean>
	
	<!-- OTP AuthenticationProvider -->
	<bean id="otpAuthenticationProvider" class="kr.or.koroad.auth.security.provider.OtpAuthenticationProvider">
		<!-- <property name="userDetailsService" ref="allbaroUserDetailsService" /> -->
		<property name="otpService" ref="otpService" />
	</bean>
	
	<!-- OTP 인증 필터: 검증 성공 시 최종 핸들러로 이동해야 하므로 customSuccessHandler 사용 -->
	<bean id="otpAuthenticationFilter" class="kr.or.koroad.auth.security.filter.OtpAuthenticationFilter">
        <property name="authenticationManager" ref="authenticationManager" />
        <property name="authenticationSuccessHandler" ref="otpAuthenticationSuccesshandler" />
        <property name="authenticationFailureHandler" ref="otpAuthenticationFailureHandler" />
    </bean>
	
	<!-- 비밀번호 변경 필요 필터 -->
	<bean id="passwordChangeRequiredFilter" class="kr.or.koroad.auth.filter.PasswordChangeRequiredFilter">
		<property name="changePasswordUrl" value="/auth/change-password" />
		<property name="changePasswordProcessUrl" value="/auth/change-password/process" />
		<property name="logoutUrl" value="/auth/logout" />
	</bean>

	<bean id="signinAuthenticationSuccesshandler" class="kr.or.koroad.auth.security.handler.SigninAuthenticationSuccesshandler" />
	
	<bean id="otpAuthenticationSuccesshandler" class="kr.or.koroad.auth.security.handler.OtpAuthenticationSuccesshandler" />
	
	<bean id="ssoAuthenticationSuccessHandler" class="kr.or.koroad.auth.security.handler.SsoAuthenticationSuccessHandler">
		<property name="customSuccessHandler" ref="customSuccessHandler" />
	</bean>
	
	<bean id="customSuccessHandler" class="org.springframework.security.web.authentication.SavedRequestAwareAuthenticationSuccessHandler" />
		
	<!-- form-login 인증 실패 이후 작업 처리 -->
	<bean id="signinAuthenticationFailureHandler" class="kr.or.koroad.auth.security.handler.SigninAuthenticationFailureHandler">
		<property name="userDetailsService" ref="allbaroUserDetailsService" />
	</bean>
	
	<!-- OTP 인증 실패 이후 작업 처리 -->
	<bean id="otpAuthenticationFailureHandler" class="kr.or.koroad.auth.security.handler.OtpAuthenticationFailureHandler" />
	
	<bean id="authenticationFailureHandler" class="kr.or.koroad.auth.handler.KoroadAuthenticationFailureHandler">
		<property name="changePasswordUrl" value="/auth/change-password" />
	</bean>

	<bean id="logoutSuccessHandler" 
	      class="org.springframework.security.web.authentication.logout.SimpleUrlLogoutSuccessHandler">
	    <property name="defaultTargetUrl" value="/auth/signin?logout" />
	</bean>
</beans>